name: Test
on: [push, pull_request]
jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
      - uses: viliusddd/composite@master
    - uses: octocat/say-hello@v1
      with: 
        name: OctoCat